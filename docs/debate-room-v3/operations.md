# Operations and controlled release

Current publication, 10 September 2026 (Asia/Manila): [exact revision `af963655` was published](evidence/publication-34404153144.json) through both Workers and Pages, with 29 live asset hashes and public authorization checks. [The existing signed-in Chrome session passed an ordinary reload](evidence/live-browser-copy-af96365.json): the new room and media scripts returned 200 without disk-cache reuse, the empty lobby remained free of the timer error for 44 seconds, and the rules guide plus create/cancel controls worked without creating an event. This resolves the [recorded `44451be` returning-browser cache failure](evidence/live-browser-cache-gap-44451be.json) at that observed scope. Source/controlled-browser evidence includes 213 source pairs, 444 Debate checks and 16 accelerated stages in 80.892 seconds; it is not a full-duration hosted match. See [current publication requirements and remaining checks](publication-20260910.md). Full acceptance remains open, including the 93-minute hosted rehearsal, complete three-judge flow, private upload/download, physical media/capacity, real mail, separate production paid/unpaid journeys and the whole-site audit.

[Actual Study catalog refresh observation](evidence/study-catalog-live-verification.json) passed at `af963655`: during one 11.047-second pending refresh, the sampled six room-choice controls remained enabled while admin Add/Edit controls were disabled, then restored in the later idle sample. The current Study script URL loaded, the lobby image was absent, and the temporary tab was closed without joining or changing a room. [Read-only latency review](evidence/study-catalog-latency-source-review.json) does not establish the cause of that delay or general performance; the latency remains under investigation.

Historical hosted checkpoint, 9 September 2026. **Hosted run 34376105917 deployed exact c7b3149 and passed authenticated entry, then failed on evidence upload HTTP503 after 243.118 seconds,47 checks and66 commands. No timed stage completed. Separate [guarded reconciliation](evidence/staging-hosted-rehearsal-34376105917-reconciliation.json) removed the test event/data and ten held Auth accounts. Independent readback of all eleven original identities and the exact event at16:51:44.167761 UTC found all19 scoped counts zero. No complete hosted rehearsal or production release is claimed.**

[PR361 verification](evidence/ci-validation-ea3c1e2.json) passed all thirteen Debate suite groups, both synthetic browser journeys,35 native PostgreSQL checks/38 expected denials and11 Wrangler metadata checks, with202 exact source-hash comparisons. Candidate ea3c1e2 and merged main c7b3149 share tree a8c7a0217a64208e3082ed6bb0eeef9297a963b2. Shared validation did not run for that path set; the unrelated promotional-media HTTP404 workflow failure remains recorded. [Fresh preparation34374578615](evidence/staging-prepare-hosted-34374578615.json) passed and all eleven fixtures were independently absent across19 scoped checks at16:19:26 UTC.

[Hosted run34376105917](evidence/staging-hosted-rehearsal-34376105917.json) deployed c7b3149f3f690f9ded2b4b44e80336f04b47cfda as Worker d10a5718-7dcd-4a0e-a356-be8c31634381 (deployment4f8b840b-13dc-4d73-94dc-f9f19e82346e), fingerprint fb7e42a4be59d5b9bd1a31dd4c716b0ca9e68c77784cfe2c3e0e7dfb46017307. Configuration preservation and authenticated asset/access smoke passed. All46 observed events/discovery bootstraps returned200; the browser created/reloaded the event, configured six speakers and reached15-minute preparation READY. It then received503 on the evidence upload and timed out waiting for the dependent share command. The source error behind that503 was not captured; the preceding run's403 remains unclassified. All eleven sessions were fenced, but the runner held ten event-member accounts after an order-sensitive cleanup comparison failed. Root subsequently used the unchanged native snapshot/CAS helper to remove the exact event plus66 receipts,66 audit rows,19 match versions,one failed upload,one cleanup job and26 rate rows. Storage object metadata was absent and the helper rechecked it; a separate Storage API byte-absence probe was not performed. Separate [guarded reconciliation](evidence/staging-hosted-rehearsal-34376105917-reconciliation.json) removed the test event/data and ten held Auth accounts. Independent readback of all eleven original identities and the exact event at16:51:44.167761 UTC found all19 scoped counts zero. Media, real mail and public access were disabled in that restricted attempt.

[Exact synthetic export review](evidence/export-visual-ci-ea3c1e2.json) inspected all nine pages across five PDFs and parsed the40-row CSV; all six hashes matched. [Study admission source audit](evidence/study-admission-audit-c7b3149.json) records30 focused passing tests and the exact-room active-admin/manual-admission/explicit-entry contract. Follow-up source changes stabilize complete cleanup row ordering, add safe immediate upload-failure diagnostics, and expand synthetic Library/new-room approval coverage. That checkpoint preceded the later clean isolated CI and code publication recorded in the current publication receipt; those later checks do not prove successful hosted file delivery.

[Actual c7 hosted image review](evidence/hosted-browser-visual-review-34376105917.json) inspected five captured PNGs and found 320px Pin/text overlap and offscreen media controls. The local CSS follow-up wraps controls and separates the participant rail/tile rows, with synthetic and hosted geometry/hit-target assertions. At that checkpoint, full isolated CI was pending: all thirteen local groups passed, but the aggregate recorded SOURCE_CHANGED_DURING_RUN while assertions were being updated. This historical failed aggregate is retained; the later clean controlled-browser and pixel evidence is linked in the current publication receipt. The subsequent ordinary-reload lobby check resolves the recorded script-cache failure without proving a live active match.

Historical checkpoint: candidate `9a5991d` passed all feature and shared CI and merged as `e9e6608302b78649af3409febb06ed6f16e6c36e`. The [exact helper upgrade](evidence/staging-helper-event-id-upgrade-20260909.json) was applied to staging as migration `20260909150103`; body, ACL and five actual permission probes passed. [Separate cleanup of failed run 34363892777](evidence/staging-hosted-rehearsal-34363892777-reconciliation.json) completed, with all 19 scoped counts zero at 15:03:14 UTC. [Fresh preparation 34367902899](evidence/staging-prepare-hosted-34367902899.json) passed, with independent exact-account absence verified. Historical failures below retain their original outcomes.

[Hosted run 34369187490](evidence/staging-hosted-rehearsal-34369187490.json) completed as a failed rehearsal on exact `e9e6608302b78649af3409febb06ed6f16e6c36e`. Deployment preservation and authenticated asset/access smoke passed. That historical attempt served Worker `b07f10ed-c846-4b3e-9ac6-edcf6543ff70`, fingerprint `aa7996a73493b2cce8419a0189ee5cf9ae8b4c72d5869385c7b8fffcff333cab`; post-failure capture matched. The browser created one event, then timed out waiting for its title after reload; the single later `/debate-room/events` 403 remains unclassified. It ran for **27.747 seconds**, with zero successful checks or timed stages. Actual immediate Auth/Worker logout fencing and populated helper cleanup passed: all eleven identities and the event plus its receipt, audit and rate row were removed. Independent exact-ID/event readback at **15:33:26.119976 UTC** found all **19 scopes zero**. The permanent receipt retains thirteen reports and 141 verified source-hash pairs. Real-duration completion, physical media, long-session refresh and full acceptance remain open; media and real mail stayed disabled.

Controlled staging work has occurred in Supabase project `hlzqmreeoghbldnhlybr` and Worker `duediligence-examinations-staging`. The [two additive migrations, hosted rollback and permission checks](evidence/staging-database-20260909.json) are recorded. The [staging-only cleanup helper](evidence/staging-hosted-cleanup-helper-20260909.json) was also installed after native PostgreSQL CI; its migration-ledger bytes and both function bodies match the reviewed source, and actual unprivileged database calls are denied. The helper remains outside production migration discovery.

The [first restricted Worker deployment](evidence/staging-deploy-34352333268.json) passed configuration preservation but failed its authenticated smoke step; its temporary accounts were cleaned. That receipt does not establish the complete hosted organizer rehearsal. At that checkpoint, valid-fixture cleanup through the hosted REST API and immediate logout fencing still required evidence; their later passing results are recorded above. The full rehearsal, physical-media verification and PostgREST timeout hoisting remain unverified. No production deployment, public launch, quota purchase or real Debate email/media session is authorized by this document.

The [first hosted preparation](evidence/staging-prepare-hosted-34358904483.json) failed after one real sign-in because the fixture runner rejected the server's valid opaque 12-character refresh token and REST session metadata. No deployment or Debate event was requested. The private bucket was created and verified, and retained. A [separate exact-account reconciliation](evidence/staging-prepare-hosted-34358904483-reconciliation.json) removed the held session, refresh credential and synthetic account after guarded ownership/reference checks; an independent readback confirmed absence. The failed attempt remains failed, and its old-bearer denial and immediate logout fencing remain unverified. The revised runner retains received credentials only in memory, qualifies cleanup access with the fixed Auth server, and normalizes expiry from the authenticated JWT. At that checkpoint, fresh CI and a new successful preparation were required before another long rehearsal; their later outcomes are recorded above. The [scoped database advisor readback](evidence/staging-helper-security-advisors-20260909.json) records the existing unrelated warnings separately.

## Configuration

The later [successful eleven-account preparation](evidence/staging-prepare-hosted-34362629625.json) verified real immediate Auth/Worker logout denial, the empty-scope cleanup RPC and exact account absence. [Hosted run 34363892777](evidence/staging-hosted-rehearsal-34363892777.json) subsequently deployed `c03d122334fbf1da1373c7008b282f53bc5fceac` as Worker version `a2fcbb39-2203-48d7-9ca9-224e4c798492`; preservation, exact assets and authenticated allow/exclude smoke passed. Its then-current baseline fingerprint was `6d8026d840bc88e74cc8f0efdfa6f66f3bf389fb5c7c03f51aa31e6717e77c73`. The browser failed after creating its first event because fixture validation expected a UUID instead of the service's generated `de-` identifier. All eleven sessions were fenced; ten accounts were deleted and the host plus its event were held. This is a failed rehearsal, with no timed stage completed. The later staging-only exact-definition helper upgrade, native CI, separate held-data reconciliation and fresh preparation are recorded in the current checkpoint above. The older preparation cannot authorize a changed candidate or baseline.

Existing Supabase, LiveKit, origin validation and approved outgoing-mail infrastructure remain the integration points. Never put service-role keys, mail keys, invitation secrets or media tokens in the static artifact, audit output or screenshots.

| Setting | Safe initial state and purpose |
|---|---|
| `DEBATE_ROOM_ENABLED` | Omitted/false. Set true only for approved public release. Public release must then permit ordinary signed-in accounts independently of payment. |
| `DEBATE_PREVIEW_ACTOR_IDS` | Explicit test account UUIDs only for protected preview. Empty denies preview access. |
| `DEBATE_MEDIA_ENABLED` | Omitted/false until the provider and actual operating limits are verified. |
| `DEBATE_SWEEPER_ENABLED` | Omitted/false until migrations and the dedicated minutely scheduler are deployed and observed. Media also requires this flag. |
| `DEBATE_APPROVED_MAX_PARTICIPANTS` | No assumed value. A verified integer 1–100; all roles count. A value below100 leaves the 100-person target unverified. |
| `DEBATE_PLATFORM_OPERATOR_IDS` | Exact authenticated platform operator UUIDs, server configuration only. Default none. Does not grant ordinary users retention authority. |
| `DEBATE_RESULTS_EMAIL_MODE` / `OUTBOUND_EMAIL_MODE` | Both must explicitly permit delivery. Default suppressed. Test recipient approvals remain mandatory. |
| `DEBATE_APPROVED_REHEARSAL_RECIPIENT_IDS` | Exact approved account UUIDs for results mail; never all site users. |
| `DEBATE_INVITATION_EMAIL_MODE` | Omitted/suppressed. Explicitly enabled only for authorized invitation sends, alongside the global mail gate. |
| `DEBATE_APPROVED_REHEARSAL_RECIPIENT_EMAILS` | Exact approved email addresses for rehearsal invitations to people who may not yet be event members. |
| `DEBATE_RESULTS_EMAIL_FROM` / `DEBATE_INVITATION_EMAIL_FROM` | Verified approved sender. Invitation sender falls back to the results sender. |
| `RESEND_API_KEY` | Existing approved server-only mail credential; never in static assets or evidence. |
| `DEBATE_STORAGE_BUCKET` | Optional override of the dedicated private bucket; default `debate-private-v3`. Verify privacy on every write. |
| Private evidence/export bucket | Dedicated `debate-private-v3`; verify its private setting before writes. No public object URLs. |

These are deployment controls. Their configured values are not evidence of remaining provider capacity, functioning cron, real delivery, or approval. Inventory the actual account concurrency, participant-minutes, transfer, storage, current Study Room commitments and reconnect headroom. The planning example of100 people for63 minutes is6,300 participant-minutes before setup/preparation/judging. Do not quote it as an account allowance or peso cost.

## Additive database rollout

Review the CLI-timestamped candidates `supabase/migrations/20260909080139_debate_room_v3.sql` and `supabase/migrations/20260909080143_study_room_admission_v3.sql`. The Debate draft is the review source for its new service-only schema; the Study change retains the prior v1 RPC contracts for already-running clients. Hash the final files only after review is complete.

Apply only these reviewed migrations to the resolved disposable staging database after confirming its identity. Do not blindly push all historical migration files. Verify grants/RLS as service_role and unprivileged roles, current-token reauthorization, revision conflicts, job-claim exclusivity, current-result downloads, expired invitations, pending admission, Library permissions, cleanup/hold and orphan-file deletion. Roll back probe transactions and remove only approved synthetic fixtures and their private objects.

Take an approved backup/snapshot and record restoration instructions before production. An additive rollback first disables new Debate entry and new media joins. Keep the cleanup-capable Worker and sweeper running until every active provider session is revoked and queued lifecycle deletion is reconciled; only then restore the prior Worker/Pages releases. Retain new event/history tables for recovery and do not drop user records. Restore the old Study v1 caller when reverting its UI/backend together. Preserve the privacy of pending entrants and the Inner Chamber during rollback.

## Scheduler and monitoring

The added `* * * * *` dispatch runs only Debate maintenance. The existing two-minute scheduled operations remain on their original path. Each job keeps a durable claim and receipt; interrupted work is retried without treating a missing response as failure of the underlying competition action. Media revocation cannot be cancelled. Failed exports or email do not erase final results.

Monitor sanitized counts and ages for queued/failed jobs, overdue revocations, scheduler lag/backlog, pending uploads, storage deletions, capacity refusals, stale clocks, and provider reconnect failures. Do not log private messages, scorecard notes, email addresses or tokens. An accepted email and provider message ID establish acceptance only; inbox delivery must be separately verified. Diagnose a stalled queue before increasing concurrency or retry rate.

Lifecycle deletion jobs cannot be cancelled. A documented operator hold pauses eligible retention while keeping durable retry state. Operational/chat/evidence/official retention values are proposed preview defaults (30/30/90/365 days), requiring owner review before public launch. Cleanup needs approved policy, actual event end and no documented hold. Test deletion and retry on controlled records; do not purge unrelated site data or imply a downloaded certificate can be remotely erased.

## Staging and production sequence

1. Finish the candidate, local suite, UI walkthrough and export QA. Record the exact SHA/tree/source manifest and all unresolved ledger gates.
2. Resolve a Debate backend staging target and disposable database. The superior recovery task's `duediligence-asset-recovery-preview` and `duediligence-site-recovery` backup are excluded. Existing legacy staging workflows still reference retired Examination Room behavior and must not be used blindly or weakened to pass.
3. Prepare the target-specific reviewed configuration, apply the two additive migrations, provision/verify the private bucket within existing approved resources, then deploy the exact Worker and Pages artifact to a protected preview. Keep real mail/media suppressed until their approvals and tests are ready. Record deployment IDs, origin/config verification, exact asset hashes and authenticated positive/negative cases.
4. Complete the physical device matrix, at-least90-minute full rehearsal, actual provider revocation tests, approved capacity ramp, recipient delivery and recovery checks. Record exact limitations rather than infer them from synthetic runs.
5. Present the concrete candidate and evidence for any still-required public-launch/operating-policy approval. An email requesting Gilmar's approval is not approval.
6. After authorization and passing gates, deploy Worker first and Pages second through an approved exact-SHA workflow. Verify the actual public desktop/mobile entries, direct route, refresh/auth return, free/paid access, live assets, API authorization, Study repairs and cleanup. Keep candidate/merged/deployed/live-verified states separate in the release receipt.

The later owner-authorized public entry release is recorded in [current publication evidence](evidence/publication-34404153144.json). Fresh HTTP bytes, authorization checks and the existing owner session's ordinary-reload lobby/rules/create-cancel checks passed. The earlier disk-cache failure remains historical evidence; live match actions and paid/unpaid cohorts remain separate checks. Study controls passed the bounded pending-refresh observation above; its 11-second delay remains under investigation. The sequence above remains the full-feature operating and acceptance checklist, with media and mail disabled.
